CSS Design
Go to styles to debug in chrome dev tools
style="" - add inline styles to elements

background-color: yellow / rgb(255, 0, 0) / #ff0000
font-size: xx-large
font-weight: bolder / lighter / 100-900
color: white - rgb, hex, hsl, or name red/yellow

padding/margin - spacing elements to breathe
  -top -right -botom -left - all around the element
  0 auto - first is for vertical, 2nd for horizontal

 - by default the browser adds a bit of margin to the page
 - by default heading tags add margin to the top and bottom

font-family: 'Gill Sans', 'Gill Sans Mt', Calibri, 'Trebuchet MS', sans-serif

 - if it doesn't find the first font, it tries the second and so on
 - every browser has the serif or sans-serif fonts

<button>Like</button>

 - it's used by seo, and others to know it does interactions on page
 - while <a> it's used to navigate through websites

border-radius: 25px; - adds roundeness to elements (eg. 1rem)

display: block - take the entire width of the page (block)
display: inline - the div takes only width necessary to render the element

 - button inside a div container with display block, take the whole screen