
import styling0 from "@/data/styling/styling0.json";
import settings from "@/data/settings.json";

const stylings = {
  styling0
}

const currentStyling = settings.styling;

export default stylings[currentStyling];
